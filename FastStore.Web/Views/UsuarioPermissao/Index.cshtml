@model IEnumerable<FastStore.Domain.Entidades.UsuarioPermissao>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Permissao.Nome)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Usuario.Login)
        </th>
        <th></th>
    </tr>

    @foreach (var item in Model)
    {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Permissao.Nome)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Usuario.Login)
            </td>
            <td>
                @Html.ActionLink("Edit", "Edit", new { id = item.PermissaoUsuarioId }) |
                @Html.ActionLink("Details", "Details", new { id = item.PermissaoUsuarioId }) |
                @Html.ActionLink("Delete", "Delete", new { id = item.PermissaoUsuarioId })
            </td>
        </tr>
    }

</table>

<p>
    @Html.ActionLink("Retornar", "MenuAdmin", "Admin")
</p>

